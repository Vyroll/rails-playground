<% flash.each do |name, msg| %>
  <% name = (['primary', 'secondary', 'success', 'danger', 'warning', 'info', 'light', 'dark'].include? name) ? name : 'primary' %>
  <%= content_tag :div, msg, class: "alert alert-#{name} " %>
<% end %>
